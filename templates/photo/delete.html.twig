{% extends 'base.html.twig' %}
{% block title %}Borrado de la foto {{photo.title}}{% endblock %}
{% block main %}

    {{ parent() }}
    
    <div class="bg-light border row align-items-center justify-content-center my-4">
    	<div class="col p-3 text-center">
    		<p>Borrado de la foto <b>{{photo.title}}</b>.</p>
    		<p>Por favor, confirma la operacion de borrado mediante el formulario.</p>
    		{{ form(formulario) }}
    	</div>
    	
		<figure class="col p-3 text-center bg-light">
			<img class="rounded border movie-cover" style="max-width: 220px"
				alt="Retrato de {{photo.title}}"
				title="Retrato de {{photo.title}}"
				src="{{asset(portraits_directory~'/'~(photo.picture ?? 'default.jpg')) }}">
		</figure>    	
    </div>

{% endblock %}